/**
 * /tickets — placeholder page (Phase 2 will build the full ticket list UI)
 */
export default function TicketsPage() {
  return (
    <main className="min-h-screen bg-slate-950 flex items-center justify-center p-8">
      <div className="text-center">
        <h1 className="text-3xl font-bold text-white mb-2">🎫 Tickets</h1>
        <p className="text-slate-400">
          You&apos;re authenticated! The full ticket list UI will be built in Phase 2.
        </p>
      </div>
    </main>
  );
}
